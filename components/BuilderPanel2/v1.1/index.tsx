import "./page.scss";
import { Box, width } from "@mui/system";

const BuilderPanel = () => {
    
    return (
        <Box className="side-bar">
            
        </Box>
    );
};

export default BuilderPanel;
